#version 330 core

// Interpolated values from the vertex shaders
in vec3 texcoord;
in vec3 normal;

// Ouput data
out vec3 color;

void main(){

	vec3 col = vec3(1,1,1) * dot(normalize(vec3(1,1,1)), normal);
	// Output color = color specified in the vertex shader, 
	// interpolated between all 3 surrounding vertices
	color = col;

}